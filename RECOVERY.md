# خطة التعافي والوصول

<div dir="rtl" style="text-align: right">

**الغرض:** هذا الملف يجيب على سؤال واحد — *لو ضاع الجهاز أو غاب المشرف، ما الذي
يتعطّل، ومن أين يُستعاد؟*

`CLAUDE.md` يشرح **لماذا** الكود على ما هو عليه، ويكفي لفهم المشروع وتعديله.
لكنه لا ينفع إطلاقاً في هذا السؤال: المعرفة التصميمية موثّقة، أما المفاتيح
والحسابات فتعيش خارج المستودع بحكم `.gitignore` — وبدونها لا يمكن نشر شيء ولا
إعادة ضبط رمز رحلة ولا تشغيل أي سكربت إداري، مهما كان فهم القارئ للكود.

⚠️ **لا تضع في هذا الملف أي سرّ.** هو *خريطة* للأسرار لا نسخة منها: أين تعيش،
وكيف تُولَّد من جديد. الملف مرفوع إلى مستودع عام.

---

## ١. ما الذي يعيش خارج المستودع

هذه القائمة مشتقّة من `.gitignore` — كل ما فيها موجود على جهاز المشرف وحده.

| الملف | ما هو | هل يمكن توليده من جديد؟ |
|---|---|---|
| `serviceAccountKey.json` | مفتاح Firebase Admin SDK للإنتاج | ✅ نعم — انظر §٣ |
| `serviceAccountKey.staging.json` | نفسه لبيئة staging (إن وُجدت) | ✅ نعم |
| `.env.local` | إعداد Firebase للعميل (٦ متغيّرات) | ✅ نعم — القيم عامة بطبيعتها، انظر §٣ |

**ولا شيء منها سرّ لا يُعوَّض** — كلها تُولَّد من جديد *بشرط* امتلاك الحسابات في §٢.
الخطر الحقيقي كله في §٢، لا هنا.

## ٢. الحسابات — وهذه وحدها ما لا يُستعاد بلا وصول

⚠️ **هذا الجدول هو خريطة النجاة.** بدونه لا معنى لبقية الملف.

| الخدمة | مالك الحساب | البريد | ملاحظة |
|---|---|---|---|
| GitHub (`morsel90/Travel-Wallet`) | Mohammed Alathim | `mostqbel.morsel@gmail.com` | المالك وحده يعدّل ruleset الحماية |
| Firebase — مشروع الإنتاج `travelapp-87206` | Mohammed Alathim | `alathemmohammed@gmail.com` | |
| Vercel (استضافة الواجهة) | Mohammed Alathim | `alathemmohammed@gmail.com` | متغيّرات `VITE_*` مخزّنة في لوحته |
| نطاق مخصّص (إن وُجد) | غير محدد | غير محدد | |

**تفعيل التحقق بخطوتين على GitHub وGoogle ضروري — واحتفظ برموز الاسترداد
(recovery codes) في مكان منفصل عن الجهاز.** فقدان الوصول إلى بريد الحساب يعني
فقدان كل شيء، ولا يوجد ما يعوّضه.

🆕 **قبل التحوّل إلى مشروع تجاري:** انقل الملكية إلى حساب مؤسّسي (GitHub
Organization + Google Workspace) بدل حساب شخصي. النقل لاحقاً ممكن لكنه يمرّ
بتغيير روابط المستودع وإعادة ربط Vercel وFirebase.

## ٣. كيف يُعاد بناء كل شيء من الصفر

بامتلاك حسابات §٢، هذه الخطوات تُعيد بيئة عاملة بالكامل:

```bash
git clone https://github.com/morsel90/Travel-Wallet.git
cd Travel-Wallet && npm install        # يضبط أيضاً خطاف ما قبل الدفع
```

**١) إعداد العميل** — Firebase Console › Project settings › Your apps › إعداد
الويب. انسخ القيم الستة إلى `.env.local` بالنموذج في `.env.example`.
هذه القيم **ليست أسراراً** وتُشحن داخل حزمة JS أصلاً؛ الحماية كلها في
`firestore.rules` والدوال. (التفصيل في *Environment Variables* بـ `CLAUDE.md`.)

**٢) مفتاح الإدارة** — Firebase Console › Project settings › Service accounts ›
**Generate new private key**. احفظه باسم `serviceAccountKey.json` في جذر المشروع.
المفتاح القديم يبقى صالحاً ما لم تُبطله من الصفحة نفسها — **أبطِل مفتاح جهاز
مفقود فوراً**.

**٣) صلاحية المسؤول** — تُمنح بالمفتاح أعلاه بعد أن يسجّل المستخدم دخوله مرة:

```bash
node scripts/set-admin.mjs grant <email>
```

**٤) النشر** — ثلاثة أنظمة منفصلة، انظر *Deployment* في `CLAUDE.md`:

```bash
npx firebase deploy --only firestore:rules,functions
vercel --prod
```

⚠️ متغيّرات `VITE_*` يجب أن تكون مضبوطة في لوحة Vercel قبل النشر — Vite يستبدلها
وقت البناء لا وقت التشغيل، فإضافتها بعد النشر لا تفعل شيئاً حتى تُعيد النشر.

## ٤. ما لا يُستعاد بأي حال

| العنصر | لماذا |
|---|---|
| **رموز الرحلات (PINs)** | تُخزَّن كتجزئة (hash) مملّحة فقط، ولا تُعرض بعد ضبطها. لا تُستعاد — تُستبدل: لوحة الإدارة › الرحلة › رمز الدخول، وهذا يُخرج كل أعضاء الرحلة ويطالبهم بالرمز الجديد مرة واحدة. |
| **بيانات Firestore** | لا نسخ احتياطي مُعدّ اليوم. انظر التحذير أدناه. |

⚠️ **لا يوجد نسخ احتياطي لقاعدة البيانات.** المصاريف والمسافرون وسجلّات الإيداع
تعيش في Firestore بلا أي تصدير دوري. حذف المشروع أو خطأ إداري يفقدها نهائياً —
والحذف اللين في التطبيق لا يحمي من ذلك، فهو داخل قاعدة البيانات نفسها.
تصدير Excel من داخل التطبيق نسخة يدوية جزئية لرحلة واحدة، لا نسخاً احتياطياً.

**قبل أي استخدام تجاري هذا أول ما يجب حلّه** — Firestore يدعم التصدير المجدول
إلى Cloud Storage، وهو الحل المناسب هنا.

## ٥. مراجعة دورية

هذا الملف يصلح ما دام صادقاً. راجعه عند: تغيير أي حساب أو بريد، أو إضافة خدمة
خارجية جديدة، أو تغيير أي مسار في §٣. وتحقّق مرة كل سنة على الأقل أن الخطوات
ما زالت تعمل فعلاً — خطة تعافٍ لم تُجرَّب ليست خطة (القاعدة ١٨ في `CLAUDE.md`:
تحقّق من الحالة السالبة).

</div>
